# TOS_DISCLAIMER_VERIFICATION.md

**Feature:** Trilingual Terms of Use + registration acceptance + match liability disclaimer
**Branch:** `spices` · **Date:** 2026-07-19
**Targets:** `spices.govihublk.com` (app) **and** `govihublk.com` (umbrella marketing site)
**Current version: ToS v1.1** — v1.0 shipped first; see §9 for why it was superseded the same day.

Companion docs: [`TOS_AUDIT.md`](TOS_AUDIT.md) (STEP 0) ·
[`docs/agent-legibility/2026-07-19-tos-claims-truth-layer.md`](docs/agent-legibility/2026-07-19-tos-claims-truth-layer.md) (truth layer) ·
[`.planning/2026-07-19-tos-match-disclaimer.md`](.planning/2026-07-19-tos-match-disclaimer.md) (plan) ·
[`TOS_SI_REVIEW.md`](TOS_SI_REVIEW.md) · [`TOS_TA_REVIEW.md`](TOS_TA_REVIEW.md) (translation review)

---

## READ THIS FIRST — known flags

1. **Legal text is v1, pending review by a Sri Lankan attorney.** Nuwan owns this follow-up.
   When sending the packet, flag **§6 at the top**: it was the one clause with a known factual
   defect and it was narrowed (see "Deviation R1" below). Every other clause is the spec text verbatim.
2. **`tos.si.md` and all Sinhala UI strings are pending Aruni's review.** Post-review edits are
   text-only — no structural or schema change, redeploy of the web image only.
3. **`tos.ta.md` has no assigned reviewer.** Additionally, `ta.json` is only **~20% translated**
   (198 of 247 strings are still `[TA]` English placeholders) — the ToS is now the most complete
   Tamil text in an otherwise-English app shell. The earlier "~40%" figure in `TOS_AUDIT.md` was wrong.
4. **`/privacy` is still a live 404**, linked from `auth/login/page.tsx:98`, and
   `auth.termsAgreement` still tells users they agreed to a Privacy Policy that does not exist.
   Both left unfixed **by explicit decision** (audit D6). Not an oversight.
5. **§14 `support@govihublk.com` accepts mail but the destination is unproven.** SES inbound
   accepts it — and accepts a garbage control address too, so it is a catch-all. Whether a human
   reads it needs the AWS console. Open item.
6. **107 non-admin users will be hard-blocked** by the modal on next login (59 farmer, 27 buyer,
   21 supplier). Intended behaviour, but it is their first contact with a legal wall in a language
   that is either pending review (SI) or largely untranslated (TA).

---

## 1. STEP 0 audit findings

Full detail in [`TOS_AUDIT.md`](TOS_AUDIT.md). Six deviations from the spec, all resolved:

| ID | Finding | Resolution |
|---|---|---|
| D1 | Spec said alembic head 011 / add 012. **Prod head was 013**; 012 and 013 both taken. Migrations live in `migrations/versions/`, not `alembic/versions/`. | Shipped as **`014`** |
| D2 | `POST /users/complete-registration` is a **second registration path** the spec did not cover — an ungated bypass to `tos_accepted_at = NULL`. | **Both paths gated** (manager decision) |
| D3 | **No match detail page exists.** Detail is `ListingDetailsModal`; accept/dismiss buttons live on the card. | short → card, full → modal (manager decision) |
| D4 | Locale keys are nested namespaces, not the spec's dotted-flat. | `tos.*` namespace; `register.tos_checkbox` → **`auth.tos_checkbox`** |
| D5 | 107 non-admin users will be blocked on next login. | Accepted; flagged above |
| D6 | `/privacy` 404 + false `auth.termsAgreement` claim. | Flag only, unfixed by decision |

**Registration contract verified live before any frontend work** (per project rule):
required fields `username, password, name, role, district, phone`; extra fields silently tolerated,
so adding `tos_accepted` was additive-safe. Structured error shape confirmed as
`detail[0] = {loc, field, code, message, offending, type}`.

## 2. Truth-layer deviations from the "FINAL" text

Full detail in the truth-layer doc. One authorized change to the English text:

**Deviation R1 — §6 narrowed.** The spec's §6 claimed crop diagnosis, advisory, weather **and match
suggestions** are "generated by software, including artificial intelligence." Verified against source:
diagnosis (`diagnosis/service.py:135`) and advisory (`advisory/service.py:115,147`) are genuine
Gemini LLM features, but **matching is a deterministic weighted score** — distance .35 / quantity .25
/ date overlap .25 / freshness .15, pure arithmetic + PostGIS (`matching/engine.py:22-26`). Zero AI.

Shipped text: *"...generated by software, including artificial intelligence **for crop diagnosis and
advisory answers**."* Protective scope unchanged; stops implying AI matching. Approved by Nuwan.

**Also verified and requiring no change:** no payment processor, escrow, commission or fee logic
exists anywhere; no produce custody or logistics; ads carry no price/bid/fee column and never boost
listings or matches. §3, §4 and §9 are fully evidence-backed.

**R3 — two Terms documents.** `govihub-umbrella/public/terms.html` already publishes terms for the
same platform. Decision: the new in-app `/terms` is canonical, and the umbrella page is pointed at it
in a **separate deploy after this one** (different stack, `docker-compose.umbrella.yml`).
**Status: NOT YET DONE — outstanding follow-up.**

## 3. Implementation

### Backend
| Change | File |
|---|---|
| Migration 014 — two nullable columns, no backfill | `migrations/versions/014_add_tos_acceptance.py` |
| `TOS_VERSION = "1.0"` | `app/config.py` |
| `FieldError` base + `TosNotAcceptedError`; handler reads `inner.field` | `app/auth/beta_schemas.py`, `app/main.py:141,145` |
| `tos_accepted` required; server stamps acceptance | `app/auth/beta_router.py` |
| `complete-registration` gated (D2); `POST /users/me/accept-tos`; fields on `/users/me` | `app/users/router.py`, `app/users/schemas.py` |

**Bug caught during implementation — worth recording.** The first implementation used
`Field(default=False)`. Pydantic v2 **does not run validators on defaults**, so an *omitted*
`tos_accepted` parsed as `False` and registration proceeded **ungated** — only an explicit `false`
was rejected. That is the exact case an older client hits. Fixed with `validate_default=True`.
Caught by running the endpoint rather than trusting the code.

### Frontend
| Change | File |
|---|---|
| Public terms route, inline markdown renderer (no new dependency) | `src/app/[locale]/terms/page.tsx` |
| Shared disclaimer component | `src/components/ui/MatchDisclaimer.tsx` |
| `short` on cards | `farmer/matches/page.tsx`, `buyer/matches/page.tsx` |
| `full` on detail surface | `src/components/ui/ListingDetailsModal.tsx` |
| Blocking modal, admin-exempt, logout preserved | `src/components/ui/TosGateModal.tsx`, `[locale]/layout.tsx` |
| Checkbox both paths | `auth/beta-login/page.tsx`, `auth/register/page.tsx` |
| Footer terms link | `[locale]/page.tsx` |

Design notes: the terms page renders **only** the markdown, no separate `tos.title` header — the
Sinhala document title (`කොන්දේසි`) and UI title (`භාවිත නියම`) use different words and rendering
both put two words for "Terms" on one screen. The gate modal **skips `/terms` itself**, otherwise it
renders over the very document the user is being asked to accept.

### Local verification (before deploy)

```
unauth accept-tos -> 401
dev login -> 200
me has fields -> True / None
accept#1 -> 200 1.0
accept#2 idempotent -> 200 1.0
me now stamped -> True 1.0

OMITTED        422 {"field":"tos_accepted","code":"TOS_NOT_ACCEPTED",...}
EXPLICIT_FALSE 422 {"field":"tos_accepted","code":"TOS_NOT_ACCEPTED",...}
SHORT_UN       422 {"field":"username","code":"TOO_SHORT",...}   <- regression guard passed
```

Terms routes local: `/en/terms` 200 · `/si/terms` 200 · `/ta/terms` 200, correct script each.
`npx tsc --noEmit`: **0 errors outside `__tests__/`** (pre-existing missing `@types/jest` only).
Checkbox gate: unchecked → submit disabled; checked → enabled; real submit body `tos_accepted: true`.
`needsTos` empirically confirmed: NULL→True, after accept→False, admin+NULL→False, farmer v0.9→True.

---

## 4. Deploy record — spices prod, 2026-07-19

**Pre-deploy snapshot:** `/root/backups/spices-pre-tos-20260719-083854.sql` (5,108,157 bytes)

Build: `docker compose -f docker-compose.spices.yml build --no-cache govihub-api-spices govihub-web-spices`
→ both images built, **0 errors**. (No-cache is required, not optional: the web image bakes
`NEXT_PUBLIC_API_URL` at build time, so a locale/route change needs a real rebuild.)

**Migration:**
```
INFO  [alembic.runtime.migration] Running upgrade 013 -> 014, add tos acceptance columns to users
=== HEAD ===        014 (head)
=== COLUMNS ===
   column_name   |        data_type         | is_nullable
-----------------+--------------------------+-------------
 tos_accepted_at | timestamp with time zone | YES
 tos_version     | character varying        | YES
(2 rows)
=== TABLE COUNT === 28
```
Table count unchanged at 28 — columns only, no new tables, as expected.

**Infrastructure untouched:** no change to MCP container, Gunicorn worker count, or Traefik config.

### Smoke tests (live prod, real tokens)

| # | Check | Result |
|---|---|---|
| S1 | Register **without** `tos_accepted` | **422** `{"field":"tos_accepted","code":"TOS_NOT_ACCEPTED","message":"You must accept the Terms of Use to register"}` |
| S2 | Register **with** `tos_accepted:true` | **200**, token issued |
| S3 | `GET /users/me` | `tos_version: 1.0`, `tos_accepted_at` set |
| S4 | DB stamp | `tossmoke0719 \| 1.0 \| t` |
| S5 | `accept-tos` unauth | **401** |
| S5 | `accept-tos` on NULL-acceptance user | **200** `{"tos_accepted_at":"2026-07-19T08:52:56.949741+00:00","tos_version":"1.0"}` |
| S5 | `accept-tos` again (idempotent) | **200**, `1.0` |

### Public URLs + TLS

```
/terms                 200  47880b  final=/si/terms   [කොන්දේසි]
/en/terms              200  46847b  final=/en/terms   [GOVIHUB TERMS OF U]
/si/terms              200  47880b  final=/si/terms   [කොන්දේසි]
/ta/terms              200  38045b  final=/ta/terms   [விதிகள]
/api/v1/health         200     85b
/en/auth/beta-login    200  38883b
TLS OK, expires Sep  5 03:16:18 2026 GMT - 47 days left
```

`/terms` 200s **without auth** as required. Note it resolves to `/si/terms` — next-intl
`localePrefix: "always"` redirects the bare path to `defaultLocale: "si"`. This confirms the audit's
D6 correction: the pre-existing locale-less `/terms` link on the login page **self-heals**, but an
English-reading user clicking it lands on the Sinhala document. Minor, unfixed, recorded.

## 5. Playwright T1–T6 (live prod, EN + SI, real UI navigation)

Test file `e2e-v3/test-tos.js` · results `e2e-v3/results/tos-9929.json` ·
screenshots `e2e-v3/screenshots/tos/`

| Test | Verdict | Evidence |
|---|---|---|
| T1 registration blocked without ToS | **PASS** | Landing → CTA → Register tab. Unchecked → submit `disabled=true`; checked → enabled; registration reached `/en/farmer/dashboard`; DB `tos_version=1.0` |
| T2 gate modal, NULL acceptance | **PASS** | Modal shown, app behind non-interactive (verified by real click-interception probe, not a screenshot); View Terms opened `/en/terms`; Accept closed it; **reload did not bring it back** |
| T3 match disclaimers (EN) | **PASS** | 2 real match cards; `disclaimer_short` on cards, `disclaimer_full` in `ListingDetailsModal` via 🔍 |
| T3 match disclaimers (SI) | **PASS** | Sinhala glyphs render, **no tofu** — `008_T3_si_details_modal.png` |
| T4 `/terms` via footer link | **PASS** | Clicked the footer link, not a typed URL → `/en/terms`. Rendered en/si/ta, no tofu. Screenshots `009_T4_terms_en.png`, `010_T4_terms_si.png`, `011_T4_terms_ta.png` |
| T5 admin exempt | **PASS (live)** | Admin reached `/en/admin/dashboard` with no gate — see security finding below |
| T6 SI registration labels | **PASS, with caveat** | Sinhala checkbox label renders, no tofu. **Caveat below.** |

**T6 caveat — `auth.tos_error` is unreachable through the UI.** Because submit is disabled whenever
the box is unchecked, the `if (!regTosAccepted) setRegTosError(...)` branch at
`beta-login/page.tsx:238` can never fire, and implicit form submission is suppressed by the disabled
default button. The Sinhala string exists and is correct in the bundle, but it was **not observed
rendering** and was not faked. The guard is dead defense-in-depth; the real UX is the disabled
button. The spec's T1 wording allows either behaviour ("submit is blocked **or** `register.tos_error`
shows"), so this satisfies the requirement — but the error string is effectively unused.

## 6. Commits

Branch `spices`, pushed to `origin/spices`. Base `3e38975` → head `5843110`.

| Hash | Subject |
|---|---|
| `bcfc883` | feat(legal): migration 014 — users.tos_accepted_at, users.tos_version |
| `f8e1906` | feat(legal): TOS_NOT_ACCEPTED structured error + acceptance stamped on beta register |
| `0b566f7` | feat(legal): POST /users/me/accept-tos, tos fields on /users/me, gate complete-registration |
| `6a3a2be` | feat(legal): Terms of Use EN/SI/TA + trilingual locale keys + review docs |
| `643bd15` | feat(legal): match liability disclaimer on match card and detail |
| `5843110` | feat(legal): ToS checkbox on both registration paths + blocking re-acceptance modal |

**Deviation from spec Part 7:** the spec asked for exactly two commits. Six granular commits were
made instead, each independently verified and revertable — which matters for the rollback path,
since "revert the registration validation commit" is only possible if that change is its own commit.
Squashing after the fact would require `git reset`, which the project rules forbid without explicit
per-operation authorization. The two commit messages the spec specified are preserved as the
subjects of `643bd15` and (in substance) `5843110`.

## 6b. SECURITY FINDING — unrelated to this work, needs action

`e2e-v3/test-all.js:20` contains a **live production admin credential in plaintext**, in a
**git-tracked file** pushed to GitHub:

```js
const ADMIN = { username: 'nuwan', password: '<redacted — see file>' };
```

Verified present and tracked (`git ls-files` confirms). This grants full admin access to
`spices.govihublk.com`. Also in the same file: five shared-password e2e user accounts.

**Not fixed here** — rotating the password is an account change requiring explicit authorization,
and it would break the existing e2e harness. Recommended: rotate the admin password, move
credentials to env vars, and treat the old value as compromised since it is in git history
(removing it from HEAD alone is insufficient).

## 7. Pre-existing issues found in passing — NOT fixed

1. **`POST /auth/beta/register` returns 500, not 409, on `uq_users_phone_role` collision** —
   a duplicate phone+role leaks a raw `IntegrityError`. Pre-existing, unrelated to this work.
2. **ESLint is unconfigured** (`next lint` prompts for setup).
3. `.env.frontend` points at `localhost:8002` while `docker-compose.dev.local.yml` remaps the API to
   **8012**, so the browser cannot reach the API in local dev. Local-only; prod bakes the correct URL.
4. `privacy.html` claims accounts are *"hard-deleted"* but `users/models.py:47` `deleted_at` is a
   **soft delete**; it also broadly denies advertising use while ads are first-party targeted by role
   and district with per-user impression logging.
5. Diagnosis UI shows `detectedDisease` + "High Confidence" with **no standing disclaimer**, which
   sits awkwardly against ToS §6's "guidance only". Advisory UI has no disclaimer either.

## 7b. Test data left in PRODUCTION — needs a cleanup decision

Verifying registration end-to-end necessarily creates real rows in the production database. The
following were created by the smoke tests and Playwright runs and are **still present**:

- **10 users:** `tossmoke0719`, `tosplay9039a`, `tosplay9039b`, `tosplay3252buy`, `tosplay3252frm`,
  `tosplay1647b`, `tosplay9929a`, `tosplay9929b`, `tosplay9929buy`, `tosplay9929frm`
- **2 harvest listings, 2 demand postings, 4 matches** attached to the `tosplay*` accounts

T3 required a farmer with actual matches. The 9 pre-existing matches all belong to real users whose
passwords we do not have, and logging into a real user's account was not acceptable — so a match was
built by driving the real UI, which is what produced the listings and demands above.

### CLEANED UP — authorized by Nuwan, executed 2026-07-19 16:09 UTC

Pre-cleanup snapshot: `/root/backups/spices-pre-testdata-cleanup-20260719-160923.sql` (5,136,459 bytes)

`users` turned out to have **23 FK references**, not the naive four — so the cleanup script covers
`ad_events, advertisements, advisory_questions, beta_feedback, crop_diagnoses,
farmer_crop_selections, google_accounts, notification_preferences, notifications, refresh_tokens,
role_changes, weather_alerts`, the three `*_listings`/`demand_postings` owner columns, the three
profile tables, and nulls the `removed_by` moderation back-references. Script: `/root/cleanup_testdata.sql`.

Two safety measures: an in-transaction assertion that **aborts** if any match ties test data to a
real user's listing (it passed — all 4 test matches were test↔test), and a full **dry run ending in
ROLLBACK** before the committed run.

**A real check that mattered:** 126 users existed pre-cleanup vs the 113 counted at audit. The gap
was not test data — **three genuine pilot users registered during the session**:

| username | registered (UTC) | ToS state |
|---|---|---|
| `sakuralankaent` | 06:30 — **before** deploy | NULL → will correctly get the modal |
| `inoj` | 11:54 — after deploy | **v1.0, accepted** |
| `dinesh1985` | 14:50 — after deploy | **v1.0, accepted** |

`inoj` and `dinesh1985` accepted the Terms through the real UI with no involvement from testing.
That is stronger evidence the feature works than any synthetic test in this document.

**Result:** 10 test users and all dependents removed. `leftover_test = 0`, `orphan_harvests = 0`,
`orphan_matches = 0`. Population reconciles exactly — farmer 59→60, buyer 27→29, supplier 21,
admin 6 = **116**.

Post-cleanup smoke: `/api/v1/health` 200 · `/en/terms` 200 · `/si/terms` 200 · `/ta/terms` 200 ·
`/en/auth/beta-login` 200 · `/` 200 · registration gate still returns **422 TOS_NOT_ACCEPTED**.

## 9. ToS v1.1 + umbrella site (same day, after v1.0 shipped)

**Trigger:** applying the canonical Terms to `govihublk.com/terms` surfaced that the existing
umbrella draft and v1.0 conflicted **in both directions** — so a straight copy would have been a
regression, not a fix.

### What the old umbrella draft had that v1.0 did NOT

| Clause | Why it mattered |
|---|---|
| Content ownership + licence | GoviHub displays farmer listings and photos. v1.0 gave **no permission to do so**. Dropping this was a real legal gap, not a nicety. |
| Age requirement (16+) | No eligibility floor in v1.0 |
| Prohibited uses | No scraping / impersonation / harassment rules in v1.0 |

### What v1.0 fixed in the old umbrella draft

- *"matching suggestions are **AI-generated**"* — the **identical over-claim** corrected in the app
  (R1). Confirms the problem was **systemic across surfaces**, not a one-off in the spec.
- Liability capped at *"fees paid in the preceding 12 months"* with **no carve-out for death,
  personal injury, or fraud** — precisely what the Unfair Contract Terms Act voids. v1.1 §11 restores it.
- *"courts of Colombo"* → now courts of Sri Lanka.
- A publicly rendered **"Note to counsel: Draft terms pending review"** on both `/terms` and
  `/privacy` — visible to real users. Removed from both.

### Resolution — v1.1 (manager decision: merge, ship both)

Three clauses merged in; sections renumbered **14 → 17**. New `§5 Who Can Use GoviHub`,
`§7 Your Content`, `§8 Prohibited Uses`. AI clause is now §9 (bounded scope intact); UCTA carve-out
is now §11 (intact). SI/TA updated as a **delta** so already-reviewed v1.0 prose was not regressed.

`TOS_VERSION` 1.0 → 1.1 in backend (`app/config.py`) and frontend (`TosGateModal.tsx`).
**This re-gates every user who accepted 1.0**, including the two real users who accepted earlier
today. Correct per §16 — adding a content licence is a material change.

### Umbrella deploy (`docker-compose.umbrella.yml`, separate stack)

Not a git repo — files are `scp`'d to `/opt/govihub-umbrella/public/`, then the image is rebuilt
because the Dockerfile bakes `public/` in. `build --no-cache` + `up -d --force-recreate`.

**Bug found and fixed during deploy:** `/terms-si` returned **404 despite the file being in the
image**. The umbrella nginx uses **explicit `location =` blocks per clean URL** with a catch-all
`return 404` — there is no generic `try_files` fallback, so a new page is unreachable until a block
is added. Fixed in `govihub-umbrella/nginx.conf`, rebuilt, verified.

Also: the three Sinhala pages (`si.html`, `about-si.html`, `contact-si.html`) linked to the **English**
`/terms`; repointed to `/terms-si`. English pages left on `/terms`.

**Umbrella verification (live):**
```
/terms       200  h2=17  si_chars=2
/terms-si    200  h2=17  si_chars=3867
/privacy     200  h2=8   si_chars=0
/about       200  h2=3   /about-si  200  h2=3
/contact     200  h2=1   /si        200  h2=3   /  200
```
Stale-content scan on `/terms`: `matching suggestions are AI-generated`, `Note to counsel`,
`preceding 12 months`, `courts of Colombo` — **all absent**. v1.1 markers present 4/4.

### Spices v1.1 deploy (live)

`build --no-cache` both services, 0 errors; api + web both recreated on fresh images.

```
/en/terms  200  h2=17  v1.1=True  new_clauses=3/3
/si/terms  200  h2=17  v1.1=True
/ta/terms  200  h2=17  v1.1=True
registration gate -> 422 TOS_NOT_ACCEPTED
backend settings.TOS_VERSION -> 1.1
```
(SI/TA report 0/3 on the English clause markers because those strings are translated — expected,
not a gap. The 17-section count is the structural check that matters.)

**Who the v1.1 gate now catches** — measured, not estimated:

| tos_version | non-admin | admin |
|---|---|---|
| `1.0` | **2** (re-gated by the bump) | 0 |
| never accepted | **108** | 6 (exempt) |

**110 non-admin users** will see the modal on next load; the 6 admins never will. The 2 at v1.0 are
the real users who accepted earlier today — they will be asked once more, which is the intended and
correct consequence of a material change under §16.

### Still open after v1.1

- **`/privacy` on the umbrella is untouched and still contradicts the code** — claims accounts are
  hard-deleted when `deleted_at` is a soft delete, and broadly denies advertising use of data while
  ads are first-party targeted by role and district. Only the "Note to counsel" came out.
- **The in-app `/privacy` route still 404s** (audit D6, unchanged).
- Sinhala privacy page does not exist; SI pages still link to the English `/privacy`.
- Tamil has no umbrella page — §15 claims publication in three languages, true of the app, not of
  the marketing site.

## 8. Rollback

- **Frontend/modal breakage:** redeploy the previous web image.
- **Backend breakage:** revert the registration validation commit, redeploy api.
- **Do NOT drop the columns.** They are additive and safe to leave; dropping them destroys the
  acceptance record of every user who already accepted.
